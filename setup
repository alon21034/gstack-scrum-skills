#!/usr/bin/env bash
# setup — install sprint skill and helper scripts

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

SKILL_ROOT="${SKILL_ROOT:-}"
COMMANDS_DIR="${COMMANDS_DIR:-}"
INSTALL_HOST="${SETUP_HOST:-auto}"
DRY_RUN=false

is_valid_host() {
  local host="$1"
  case "$host" in
    auto|claude|codex) return 0 ;;
    *) return 1 ;;
  esac
}

default_skill_root() {
  local host="${1:-auto}"
  case "$host" in
    codex)
      printf "%s\n" "$HOME/.codex/skills/sprint"
      ;;
    claude)
      printf "%s\n" "$HOME/.claude/skills/sprint"
      ;;
    auto)
      if [[ -d "$HOME/.codex/skills" ]]; then
        printf "%s\n" "$HOME/.codex/skills/sprint"
      elif [[ -d "$HOME/.claude/skills" ]]; then
        printf "%s\n" "$HOME/.claude/skills/sprint"
      else
        printf "%s\n" "$HOME/.codex/skills/sprint"
      fi
      ;;
  esac
}

default_commands_dir() {
  local host="${1:-auto}"
  case "$host" in
    codex)
      printf "%s\n" "$HOME/.codex/commands"
      ;;
    claude)
      printf "%s\n" "$HOME/.claude/commands"
      ;;
    auto)
      if [[ -d "$HOME/.codex/commands" ]]; then
        printf "%s\n" "$HOME/.codex/commands"
        return
      fi
      if [[ -d "$HOME/.claude/commands" ]]; then
        printf "%s\n" "$HOME/.claude/commands"
        return
      fi
      printf "%s\n" "$HOME/.codex/commands"
      ;;
  esac
}

usage() {
  cat <<'EOF'
Usage: ./setup [options]

Options:
  --host <auto|claude|codex>  Host to target for default paths (default: auto)
  --skill-root <path>    install root for sprint skill/bin (default: ~/.codex/skills/sprint, host-aware)
  --commands-dir <path>  commands directory (default: ~/.codex/commands, fallback ~/.claude/commands)
  --dry-run              Print actions without writing files
  -h, --help             Show this help

Environment overrides:
  SETUP_HOST             same as --host
  SKILL_ROOT             same as --skill-root
  COMMANDS_DIR           same as --commands-dir
EOF
}

expand_path() {
  local path="$1"
  if [[ "$path" == "~/"* ]]; then
    printf "%s/%s\n" "$HOME" "${path#~/}"
  else
    printf "%s\n" "$path"
  fi
}

run() {
  if $DRY_RUN; then
    printf "[dry-run] %q" "$1"
    shift
    for arg in "$@"; do
      printf " %q" "$arg"
    done
    printf "\n"
    return 0
  fi
  "$@"
}

while [[ $# -gt 0 ]]; do
  case "$1" in
    --host)
      [[ $# -ge 2 ]] || { echo "ERROR: --host requires a value" >&2; exit 1; }
      INSTALL_HOST="$2"
      if ! is_valid_host "$INSTALL_HOST"; then
        echo "ERROR: invalid --host value: $INSTALL_HOST (expected auto|claude|codex)" >&2
        exit 1
      fi
      shift 2
      ;;
    --skill-root)
      [[ $# -ge 2 ]] || { echo "ERROR: --skill-root requires a value" >&2; exit 1; }
      SKILL_ROOT="$2"
      shift 2
      ;;
    --commands-dir)
      [[ $# -ge 2 ]] || { echo "ERROR: --commands-dir requires a value" >&2; exit 1; }
      COMMANDS_DIR="$2"
      shift 2
      ;;
    --dry-run)
      DRY_RUN=true
      shift
      ;;
    -h|--help)
      usage
      exit 0
      ;;
    *)
      if [[ "$1" = "--g""stack-root" ]]; then
        echo "ERROR: --g""stack-root was removed. Use --skill-root instead." >&2
        exit 1
      fi
      echo "ERROR: unknown option: $1" >&2
      usage
      exit 1
      ;;
  esac
done

if ! is_valid_host "$INSTALL_HOST"; then
  echo "ERROR: invalid SETUP_HOST value: $INSTALL_HOST (expected auto|claude|codex)" >&2
  exit 1
fi

if [[ -z "$SKILL_ROOT" ]]; then
  SKILL_ROOT="$(default_skill_root "$INSTALL_HOST")"
fi
if [[ -z "$COMMANDS_DIR" ]]; then
  COMMANDS_DIR="$(default_commands_dir "$INSTALL_HOST")"
fi

SKILL_ROOT="$(expand_path "$SKILL_ROOT")"
COMMANDS_DIR="$(expand_path "$COMMANDS_DIR")"

REQUIRED_FILES=(
  "sprint/SKILL.md"
  "sprint/SKILL.md.tmpl"
  "bin/sprint-setup"
  "bin/sprint-board"
  "bin/sprint-finish"
  "commands/sprint.md"
  "commands/sprint-task.md"
  "commands/sprint-board.md"
  "commands/sprint-finish.md"
)

for rel in "${REQUIRED_FILES[@]}"; do
  if [[ ! -f "$SCRIPT_DIR/$rel" ]]; then
    echo "ERROR: missing required file: $SCRIPT_DIR/$rel" >&2
    exit 1
  fi
done

run mkdir -p "$SKILL_ROOT/sprint" "$SKILL_ROOT/bin" "$COMMANDS_DIR"

run install -m 0644 "$SCRIPT_DIR/sprint/SKILL.md" "$SKILL_ROOT/sprint/SKILL.md"
run install -m 0644 "$SCRIPT_DIR/sprint/SKILL.md.tmpl" "$SKILL_ROOT/sprint/SKILL.md.tmpl"

run install -m 0755 "$SCRIPT_DIR/bin/sprint-setup" "$SKILL_ROOT/bin/sprint-setup"
run install -m 0755 "$SCRIPT_DIR/bin/sprint-board" "$SKILL_ROOT/bin/sprint-board"
run install -m 0755 "$SCRIPT_DIR/bin/sprint-finish" "$SKILL_ROOT/bin/sprint-finish"

run install -m 0644 "$SCRIPT_DIR/commands/sprint.md" "$COMMANDS_DIR/sprint.md"
run install -m 0644 "$SCRIPT_DIR/commands/sprint-board.md" "$COMMANDS_DIR/sprint-board.md"
run install -m 0644 "$SCRIPT_DIR/commands/sprint-finish.md" "$COMMANDS_DIR/sprint-finish.md"
run install -m 0644 "$SCRIPT_DIR/commands/sprint-task.md" "$COMMANDS_DIR/sprint-task.md"

if $DRY_RUN; then
  echo "Dry run complete."
  exit 0
fi

cat <<EOF
Install complete.

Installed into:
  host mode:   $INSTALL_HOST
  skill root:  $SKILL_ROOT
  commands:    $COMMANDS_DIR

Quick checks:
  ls $SKILL_ROOT/bin/sprint-{setup,board,finish}
  ls $SKILL_ROOT/sprint/SKILL.md
  ls $COMMANDS_DIR/sprint{,-task,-board,-finish}.md
EOF
